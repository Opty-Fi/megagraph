type PricePerFullShare @entity {
  id: ID!
  blockNumber: BigInt!
  timestamp: BigInt!
  pricePerFullShare: BigInt!
  token: String!
  vault: String!
}

type Balance @entity {
  id: ID!
  balance: BigInt!
  blockNumber: BigInt!
  timestamp: BigInt!
  vault: String!
  token: String!
}