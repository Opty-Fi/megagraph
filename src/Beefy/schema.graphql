type BeefyVaultData @entity {
  id: ID!
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  vault: Bytes!
  name: String
  decimals: Int
  balance: BigDecimal
  virtualPrice: BigDecimal
  token: Bytes
}
